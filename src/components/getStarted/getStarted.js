import React from 'react';
import HeroBanner from '../heroBanner/heroBanner';
const GetStarted = (props) => (
    <React.Fragment>
        <div className="home-content">
            <HeroBanner displayText="HELLO"/>
        </div>
    </React.Fragment>
);

export default GetStarted;